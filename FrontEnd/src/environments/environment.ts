// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCxci8i9oxirx_S0DhwNfpFLrvPXL61Dgc",
  authDomain: "gad-parroquial-uyumbicho.firebaseapp.com",
  projectId: "gad-parroquial-uyumbicho",
  storageBucket: "gad-parroquial-uyumbicho.appspot.com",
  messagingSenderId: "818910871207",
  appId: "1:818910871207:web:1d5aa302fe5fea7d18e582"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
